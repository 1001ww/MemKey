# 安全说明

本文档描述 MemKey 的加密体系、威胁模型与已知限制。

## 加密体系

| 环节 | 实现 |
|---|---|
| 加密算法 | AES-256-GCM（认证加密，防篡改） |
| 密钥派生 | PBKDF2-SHA256，600,000 轮迭代，128-bit 随机盐 |
| 密钥体系 | v2 信封：随机 256-bit **数据密钥（DK）** 加密整库；主密码与恢复码各自经 PBKDF2 派生后**包裹 DK**。修改主密码只重新包裹 DK，恢复码信封不随主密码变化 |
| 随机源 | 浏览器 `crypto.getRandomValues` / `crypto.subtle`（CSPRNG） |
| 加密范围 | **全部字段**：账号名称、用户名、密码、密码历史、网址、备注、分类设置、标签、TOTP 密钥——仅 KDF 参数（salt/迭代数）、IV、主密码提示（明文可选）为明文 |
| 密钥存储 | 密钥仅存在于浏览器内存会话中，**从不持久化、从不上传** |

### 密码库外壳格式

```
v2（当前）
{
  v: 2,
  kdf:   { name, iterations, salt },        // 主密码 KDF 参数
  wpw:   { iv, data },                      // 主密码密钥包裹的 DK
  vault: { iv, data },                      // DK 加密的密码库 JSON（全字段密文）
  rec?:  { kdf: {…}, iv, data },            // 恢复码密钥包裹的 DK（可选）
  hint?: "主密码提示（明文，可选）"
}

v1（旧版，解锁时自动迁移）
{ kdf: {…}, iv, data }                      // 整库直接由主密码密钥加密
```

### 恢复码

- 20 位随机码，字符表去除易混淆的 `I L O 0 1`（31 字符 × 20 位 ≈ 99 bit 熵）
- 恢复码经 PBKDF2-SHA256 **100,000 轮**派生后包裹 DK；迭代数低于主密码（600k）是刻意的：恢复码是高熵随机串，暴力穷举不可行，而更低的轮数让恢复解锁更快
- 恢复码仅生成时完整显示一次，服务端与磁盘上只有其包裹信封；重新生成会使旧码立即失效
- 恢复码解锁后可直接解密密码库，因此它与主密码同等敏感，请物理隔离保存

### 主密码提示

- 提示以**明文**存储在密码库外壳中（与 KDF 参数同级），任何能读到 `data/vault.enc` 的人都能看到——它设计上就是给「忘了密码的人」看的
- 请勿在提示中写入能直接推出密码的内容；不需要时可在设置中清除

## 零知识架构

所有加密与解密在浏览器内完成。本地 Node 服务只处理密文的 JSON 外壳，不执行任何解密，共提供以下能力：

1. `GET /api/vault` —— 读取密文文件并原样返回
2. `PUT /api/vault` —— 接收请求体，解析 JSON 并仅校验外壳结构（v1：`kdf.salt` / `iv` / `data` 存在且为字符串；v2：`kdf.salt` / `wpw` / `vault` 存在且为字符串；`rec` / `hint` 等可选字段不校验），校验通过后原子写入文件；不解析也不理解密文内容
3. `DELETE /api/vault` —— 删除密文文件（「清除所有数据」「重置保险库」功能使用）
4. `GET /api/meta` —— 返回应用版本与密码库文件路径等非敏感元信息
5. 静态文件托管 —— 提供 `public/` 下的前端文件

服务进程没有任何解密逻辑，`data/vault.enc` 被拷贝到任何其他机器上都只是一段无法解读的密文。

## 网络边界

- 服务**仅绑定 `127.0.0.1`**，局域网与其他主机无法连接
- 请求校验 `Host` 头必须为 `localhost` / `127.0.0.1`，防范 DNS rebinding 攻击
- 前端零第三方请求、零 CDN、零埋点，`index.html` 自包含全部逻辑

## 运行时防护

| 防护 | 说明 |
|---|---|
| 自动锁定 | 可配置 1/5/10/30 分钟无操作后锁定并清空内存明文 |
| 剪贴板自动清除 | 复制密码 30 秒后自动清空剪贴板 |
| 弱密码提醒 | 保存时检测强度并提示；安全报告页集中展示弱密码/重复密码 |
| 原子写入 | 密文先写临时文件再 rename，避免进程中断导致文件损坏 |

## 威胁模型

**能防御：**

- 密码库文件被盗（含备份文件被拷走）—— 没有主密码或恢复码无法解密（提示为明文可见，但提示不是密码）
- 离线暴力破解 —— 600,000 轮 PBKDF2 显著抬高单次猜测成本；恢复码约 99 bit 熵，穷举不可行
- 局域网横向访问 —— 服务仅监听回环地址
- 撞库/密码复用 —— 内置生成器（随机字符 / 密码短语）+ 重复密码检测

**不能防御（如实告知）：**

- 主密码**和**恢复码同时被键盘记录器/钓鱼获取 —— 二者任一即可解密，请物理隔离保存
- 保险库处于解锁状态时，本机恶意进程读取内存
- 主密码强度不足（低于 8 位会拒绝创建，但仍建议 12 位以上长密码短语）
- 物理胁迫

## 已知限制

- **单用户、单机使用**：无多用户、无云同步——这是设计取向而非缺陷；跨设备靠备份文件迁移
- **未接受第三方审计**：加密使用的是浏览器原生 Web Crypto 实现（经过各大浏览器厂商持续审计），但应用层代码未做外部审计
- **供应链**：零 npm 依赖，攻击面已收敛到 Node 运行时与浏览器本身

## 漏洞反馈

如发现安全问题，请通过仓库 **Security → Report a vulnerability**（GitHub Security Advisories 私密通道）报告，请勿在公开 Issues 中张贴漏洞细节。感谢。
