# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.5.1] - 2026-09-11

### 修复

- **升级后首次保存报 HTTP 500**（含设置主密码提示、新增/编辑账号等一切写库操作）：v1.5.0 的快照文件名白名单校验误用于「新建」快照——校验逻辑要求文件已出现在目录清单中，而新快照必然不存在，导致首次写库必然失败。现拆分为两个函数：内部生成的快照 id 直接构造路径（格式白名单），恢复接口的外部 id 仍走目录清单严格校验；路径穿越负向测试保持 404

- 升级用户的密码库在本问题中**未被破坏**：「先快照后写入」的设计使失败发生在写库之前

## [1.5.0] - 2026-09-11

### 新增

- **自动加密快照**：每次成功写库前自动把当前 `vault.enc` 密文复制到 `data/snap/`，最多保留 20 份，超出自动淘汰最旧；快照保存的是服务端不解密的原始密文。设置 → 加密快照 可查看列表（时间/大小/ID）并一键恢复——恢复前会把恢复时的当前库再备份为快照，恢复后立即锁定需重新解锁。清除所有数据 / 忘记主密码重置时快照目录一并删除，不会残留可恢复密码库

- **加密回收站**：删除账号改为移入加密回收站，保留 30 天后自动清除；恢复时若与现有账号 ID 冲突自动改派新 ID。设置 → 回收站 支持恢复、永久删除、清空（永久操作均有二次确认）。回收站与账号记录一起随全库 AES-GCM 加密，活动列表、搜索与安全报告只读取未删除账号

- **安全健康度评分**：安全报告页新增 0–100 透明评分与等级（优秀/良好/一般/需改进），并列出每项扣分原因。新增「老旧密码」检测：`passwordChangedAt` 超过 180 天（新建账号即记、实际改密时更新、旧账号以更新时间迁移为基线）；侧栏角标只计弱密码、复用、老旧等实际风险，「未绑定 TOTP」仅作为建议区块展示，不计入风险

- **离线泄露检测（本地 HIBP）**：安全报告页可选择按 SHA-1 升序排列的 `hash:count` 本地文本文件（HIBP v8 ordered by hash 格式），对格式与排序抽样校验后，在浏览器内计算密码 SHA-1 并用 `File.slice()` 分块二分查找命中；只扫不重复密码，展示进度、支持取消。泄露库文件与结果只存在于当前解锁会话，锁定/改密/换文件即清除，**绝不联网、绝不上传密码或哈希**

- **CLI 命令行接口**：新增零依赖 `memkey-cli.js`，与本机服务通过 `127.0.0.1` 交互（服务端仍不接触明文）。完整 CRUD：`list` / `get`（`--reveal` 才显示密码）/ `add` / `update` / `delete`（软删除）/ `trash list|restore|purge` / `health` / `totp` / `snapshots` / `snapshot-restore`，支持 `--json` 与 `--port`。主密码默认终端隐藏输入，脚本化仅提供 `--master-password-stdin`，**绝不接受参数或环境变量中的主密码**。v1 库可解锁，首次 CLI 写入按浏览器同一规则升级 v2 并保留恢复码与提示

### 变更

- `GET/PUT/DELETE /api/vault` 与快照恢复接口引入 **ETag / If-Match 乐观锁**：新版前端与 CLI 写入都需匹配读取版本，冲突返回 409，浏览器收到 409 会锁定会话提示重新解锁，避免陈旧内存覆盖他人改动；未带条件头的旧客户端仍走兼容保存路径

- 密码历史 `changedAt` 与详情页「改密于」改用 `passwordChangedAt`（记录密码本身的变化时间，与账号更新时间区分）

- 前端加密核心、恢复码、TOTP 算法抽取到 `public/vault-crypto.js`，浏览器与 CLI 共享同一实现；`index.html` 行为与密文格式保持不变

### 修复（v1.5.0 代码审阅）

- CLI `--password-stdin` 此前被静默忽略（参数解析键名不一致），导致 `add` 以空密码创建账号；`--gen 24` 指定长度同样不生效
- CLI v1→v2 首次写入升级时错误改写 KDF 迭代数，若旧库使用非默认迭代数将导致重写后的信封永远无法解锁；现保留原外壳迭代数
- CLI 健康度弱密码口径与前端对齐（补上「长度不足 10 位」）
- 浏览器端 409 冲突与快照恢复后未刷新内存外壳/ETag，重新解锁仍以陈旧状态写入会再次冲突；现在冲突与恢复后都会重新读取服务端密文
- 「清除所有数据」「忘记主密码重置」在遇 409/500 时改为明确报错提示，不再静默失败
- HIBP 扫描在单条查找期间点「取消」不会再被进度状态覆盖，界面立即停止
- 设置页快照上限文案改读 `/api/snapshots` 返回的真实 limit，不再硬编码

### 兼容说明

- 旧版浏览器页面（v1.4.x）对本服务完全兼容：不带 If-Match 的请求不受 409 影响

- 升级后首次保存起自动生成快照；`data/snap/` 内为密文，可随整目录一起备份或删除

## [1.4.2] - 2026-09-11

### 修复（v1.4.1 代码审阅）

- 清空历史密码后详情弹窗不再被关闭动画清掉：弹窗世代号防止延迟清空误伤新弹窗，清空确认改为直接重开详情
- 恢复码生成/停用在写盘失败时回滚内存信封，避免界面显示「已启用」但用户从未见过可用恢复码
- 点选标签建议时不再因 input blur 把半成品提交成新标签
- 账号列表行恢复键盘可聚焦与 Enter/Space 打开详情
- 安全报告不再把空密码互相算作重复使用
- `GET /api/vault` 区分文件不存在与读/解析失败，前端遇损坏密文不再误入创建页以免覆盖
- 畸形 Host / 非法百分号路径不再打挂服务；超限请求体返回 413；密文写入增加 fsync

### 变更

- 增加整库审阅入口：`.opencodereview/` 规则与背景、`AGENTS.md` 说明用 `ocr scan` 审当前现状，避免把历史 diff（含已修复问题）再报一遍

## [1.4.1] - 2026-09-08

### 修复（v1.4.0 代码审阅）

- 恢复码熵描述与实现对齐：字符表为 31 字符（A–Z 去 I/L/O + 2–9），20 位约 99 bit，不再写成 29 字符 / 97 bit
- TOTP `decodeBase32` 遇非法字符（如 `1`）直接拒绝，不再静默丢弃后解出错误密钥

## [1.4.0] - 2026-09-08

### 新增

- **密码历史记录**：编辑账号修改密码时自动保留旧密码（每条上限 10 条，超出淘汰最旧记录）；详情弹窗新增「历史密码」折叠区，可展开查看、显示/复制每条历史密码（复制后 30 秒自动清除剪贴板），支持一键清空

- **密码短语生成器**：密码生成器新增「密码短语」模式——内置 EFF 短词库（1296 词），可调单词数（3–10）、分隔符（连字符/下划线/点号/空格/无）、首字母大写、末尾附加数字；熵值按词库实际大小计算（每词约 10.3 bit），嵌入编辑弹窗的生成器同步支持两种模式

- **恢复码**：创建保险库后自动生成 20 位恢复码（去混淆字符表，约 99 bit 熵，仅完整显示一次）；忘记主密码时可在解锁页「使用恢复码解锁」并重设主密码，恢复码在重设后继续有效；设置 → 安全 中可重新生成（旧码立即失效）或停用

- **主密码提示**：创建保险库时可填写可选提示（≤50 字符），设置页可随时设置/修改/清除；提示以明文保存在本机密码库外壳中，解锁页可点击查看，请勿包含敏感信息

### 变更

- 密码库外壳升级为 v2 信封格式：随机数据密钥（DK）加密整库，主密码与恢复码各自包裹 DK——修改主密码只需重新包裹 DK（库密文不变），恢复码信封不随主密码变化；v1 旧数据在解锁时自动静默迁移，无需手动操作

- 服务端 `PUT /api/vault` 外壳校验兼容 v1 / v2 两种格式

- 创建页风险提示与确认项措辞更新：主密码与恢复码都丢失时数据才无法恢复

### 修复

- 修复导入备份后无法用该备份主密码解锁的问题：导入后内存中的密文外壳未同步刷新，解锁仍按旧库的盐值派生密钥（现导入即同步）

### 兼容说明

- v1 备份文件可直接导入，首次解锁时自动迁移为 v2

- 建议整包升级（同一文件夹内 server.js 与 public/ 配套分发）；若仅替换部分文件，旧版前端无法解锁 v2 密文库，旧版前端保存也会丢失恢复码与提示字段

## [1.3.0] - 2026-09-07

### 新增

- 标签管理：设置页支持标签重命名、合并、删除、拖拽排序，以及一键清理未被任何账号使用的标签；侧栏标签顺序与设置页一致
- 两步验证（TOTP）：账号可保存验证器密钥或 otpauth 链接；详情页在密码下方显示 6 位验证码与 30 秒倒计时，列表行可一键复制

## [1.2.7] - 2026-09-04

### 优化

- 账号详情弹窗标签展示降级：标签从与用户名/密码同级的大字段行，移至弹窗底部属性行，以轻量 `#标签` 纯文本呈现，多标签时自动换行不再挤压元信息；分类 chip 补充分类自身图标（与侧栏一致），与标签一眼可区分

### 安全（v1.2.6 代码审阅修复）

- 修复侧栏分类名的存储型 XSS：分类名渲染统一经 `esc()` 转义（此前仅分类漏转义），分类增删改输入 trim 并截断 20 字符

- 修复「重置」确认输入对全角字符不生效：确认文本经 NFKC 归一化后比对，兼容全角「重置」与英文 reset

### 优化（v1.2.6 代码审阅修复）

- 版本号收敛为唯一真源：服务端 `/api/meta` 提供版本，侧栏与设置页改为运行时读取，删除前端两处硬编码，避免发版漏改

- 加密持久化改为整体序列化密码库对象，不再手工枚举字段，未来新增顶层字段不会静默丢失

- 持久化保存增加串行互斥，连续快速保存按顺序排队，消除并发读写的交错覆盖窗口

- Base64 编码改为分块处理，避免超大密码库转换时栈溢出

- `.gitignore` 增加 `.zcode/` 目录

## [1.2.6] - 2026-09-04

### 修复

- 设置页内容未居中：限宽卡片容器改用 `settings-cards` 类，增加水平自动边距

- 侧栏「标签」名称误加 `#` 前缀，现显示标签本名

- 选中具体标签时「全部账号」同时高亮：全部账号激活条件补上「无标签筛选」

- 在具体标签视图新增账号时，编辑弹窗标签未默认该标签，现自动带入当前标签

### 优化

- 标签输入由浏览器原生 datalist 改为应用内候选菜单：按输入过滤、排除已添加标签，支持右侧箭头展开、方向键选择、Escape 关闭与点击外部关闭

- 分类与自动锁定自定义下拉视觉统一：箭头右对齐、收紧间距、补全焦点态与 ARIA 状态

## [1.2.5] - 2026-09-04

### 修复

- 修复 Windows 批处理文件在中文系统代码页下乱码并导致安装/卸载报错的问题：三个用户入口改为 ASCII 内容，避免 `cmd.exe` 解析编码冲突

- 修复手动启动窗口绑定服务的问题：双击 `启动MemKey.bat` 后，服务在后台持续运行，提示窗口可直接关闭，浏览器自动打开

- 修复 Windows 开机自启不稳定的问题：`安装开机自启.bat` 检查当前用户的 Node.js PATH 后生成隐藏启动项；`卸载开机自启.bat` 可移除启动项

### 文档

- Windows 用户入口改为 `启动MemKey.bat`、`安装开机自启.bat`、`卸载开机自启.bat`

- 同步更新 README、界面版本号、服务元信息版本和本地服务提示

## [1.2.4] - 2026-09-02

### 修复

- 侧栏「安全报告」角标被拉宽（第二次修复）：v1.2.2 的 `.badge{flex:none}` 优先级低于 `span:not(.cnt)` 弹性规则而未生效，现将 `.badge` 从该选择器排除（`:not(.badge)`），实测恢复 18px 小圆角标
- 账号行点击后出现闪烁竖线（文本插入符残留）：点击行时浏览器在行文本上放置折叠选区，关闭详情弹窗后焦点回落 BODY、选区未清除，Chrome 在行标题文字末尾渲染闪烁插入符；现于 `closeModal()` 中清除文档选区，Esc / 点击遮罩 / 点 × 三种关闭方式实测均不再出现

## [1.2.3] - 2026-09-02

### 优化

- 编辑账号弹窗移除底部「取消」按钮（与右上角 × 职能重复），右下角仅保留「保存」

- 账号详情弹窗移除底部「关闭」按钮，保留「删除 / 编辑」

## \[1.2.2] - 2026-09-02

### 修复

- 弹窗右上角关闭（×）图标不显示：图标定义表中 `x`（及 `search`）两个条目自 v1.1 重写时意外丢失，`icon('x')` 渲染为空 SVG，补回定义

- 侧栏「安全报告」角标被拉宽：`span` 弹性规则误匹配角标，改为仅限直接子级文本 span 且角标固定 `flex:none`

- 密码生成器页面内容靠左：容器增加 `margin:0 auto` 居中

- 账号列表行移除手型光标、悬停位移与 tabindex 聚焦框（点击行仍可打开详情）

## \[1.2.1] - 2026-09-02

### 优化

- 重排新增/编辑账号弹窗布局，解决字段增多后弹窗过高问题：弹窗加宽至 640px；用户名与网址恢复双列；标签与分类同行；备注移至末尾并收窄；收藏开关改为标题栏星星按钮（切换计入未保存提醒）；收紧弹窗内边距与行距

## \[1.2.0] - 2026-09-02

### 新增

- **标签系统**：账号可打多个标签（每条最多 10 个），适用于「项目 × 类型」双维度组织——分类做类型（数据库/服务器/服务账号），标签做项目名（参考 1Password 方案）

- 编辑弹窗新增标签编辑器：输入回车或逗号添加，点击 × 移除，已有标签自动补全（datalist）

- 侧栏新增「标签」区：按标签筛选查看，标题栏显示 `# 标签名`

- 列表行与详情弹窗展示标签；全局搜索覆盖标签内容

- 标签随账号条目一同 AES-256-GCM 加密存储

## \[1.1.4] - 2026-09-02

### 变更

- 分类排序改为**拖拽排序**：按住分类行（左侧抓手图标）拖到目标位置，蓝色指示线提示落点；DOM 原位移动不刷新页面，顺序后台静默加密保存并同步侧栏（替代 v1.1.3 的上移/下移按钮方案，其每次点击触发整页重渲染体验不佳）

## \[1.1.3] - 2026-09-02

### 新增

- 分类管理支持排序：每行上移/下移按钮（首行/末行对应按钮自动禁用），顺序随密码库加密持久化，侧栏同步

### 优化

- 新增账号时分类默认选中侧栏当前所在分类（此前固定为「其他」）；在「全部账号/收藏/安全报告/密码生成器」视图下仍默认「其他」

## \[1.1.2] - 2026-09-02

### 新增

- 解锁页「忘记主密码？」重置流程：说明后果后输入「重置」二次确认，删除本机密码库并回到创建页（零知识架构下无找回途径，重置是唯一出口）

### 修复

- 自定义分类未包含在加密持久化载荷中，刷新/重启后分类增删改丢失（现随密码库一同加密保存）

- SECURITY.md 准确描述服务 API：补全 `DELETE /api/vault` 与 `GET /api/meta`，说明 PUT 仅校验 JSON 外壳结构而非原样字节读写

- 漏洞反馈渠道由 GitHub Issues（公开）更正为 Security Advisories 私密通道

- 界面版本号（侧栏 / 设置页）与 CHANGELOG 同步为 v1.1.2

## \[1.1.1] - 2026-09-02

### 新增

- 分类管理：设置页可增删改分类，15 种图标可选，「其他」为保留分类

- 自定义下拉组件：替换原生 select，统一浅色蓝白视觉风格

- 开源文档：README、SECURITY、LICENSE（MIT）、CHANGELOG

### 修复

- 表单文本框（名称/用户名/网址）缺失 `type` 属性导致 CSS 选择器不匹配，显示为原生裸框

- 搜索框聚焦时三层提示叠加（边框 + 光环 + outline 泄漏），收敛为单层边框高亮

- 保存账号时增加 loading 状态，防止密钥派生期间重复提交

- 密码强度文字标签随强度语义变色

## \[1.1.0] - 2026-09-02

### 重构（破坏性）

- 存储层从浏览器 localStorage 迁移到**本地文件** `data/vault.enc`（零知识：服务端只存密文）

- 新增零依赖 Node 本地服务 `server.js`（静态托管 + 密文文件 API，仅监听 127.0.0.1）

- 配套 `start.bat` / `install-startup.bat` / `uninstall-startup.bat`

### 变更

- 界面从暗色黄铜主题重做为浅色蓝白经典主题

## \[1.0.0] - 2026-09-02

### 初版

- 单文件浏览器应用（localStorage 存储）

- AES-256-GCM + PBKDF2-SHA256(600k) 浏览器端加密

- 账号增删改查 / 收藏 / 搜索 / 固定六分类

- 密码生成器、安全报告、自动锁定、剪贴板自动清除、备份导入导出

